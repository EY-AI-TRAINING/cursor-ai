# 03 — Generation notes (attempt 1)

- Files written: `tests/test_req_2481_order_cancellation.py` — 5 tests, one per SEQ-1…SEQ-5
- Fixtures used: `api`, `customer_a`, `customer_b`, `make_order` from `tests/conftest.py`
- Markers: `req` / `ac` / `seq` on every test (registered in `pytest.ini`)
- Collection: `pytest --collect-only` run locally before handoff
- Open issues: none
