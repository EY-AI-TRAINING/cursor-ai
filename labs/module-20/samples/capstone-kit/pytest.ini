[pytest]
markers =
    req(req_id): the requirement this test belongs to, e.g. REQ-2502
    ac(ac_id): the acceptance criterion this test proves, e.g. AC-3
    seq(seq_id): the test-sequence scenario this test implements, e.g. SEQ-5
