# Raw requirement (draft) — "Add rate limiting"

> **Read-only fixture for Lab 11.3.** This is the *input* for the Requirement Analysis agent. It is deliberately
> incomplete — the agent's job is to find the gaps, not to invent answers for them.

**Ticket title:** Add rate limiting to the API

**Ticket body (verbatim):**

> Our API gets hammered sometimes and we need to protect it. Add rate limiting so clients can't overload us.
> It should be configurable and work for all endpoints. Make sure it's fast and doesn't break existing clients.

**Attachments:** none. **Discussion:** none.
